name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # ─── Lint shell scripts ────────────────────────────────────────────────────
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install shellcheck
        run: sudo apt-get install -y shellcheck

      - name: Lint ai-squad CLI
        run: shellcheck -x scripts/ai-squad

      - name: Lint test scripts
        run: find tests -name "*.sh" -exec shellcheck -x {} +

  # ─── Build and test the TUI ────────────────────────────────────────────────
  build-tui:
    name: Build TUI
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: tui/go.mod
          cache-dependency-path: tui/go.sum

      - name: Build
        run: |
          cp -r template tui/template
          go build -C tui -o ../squad .

      - name: Verify binary runs
        run: ./squad --version

      - name: Cross-compile all platforms
        run: |
          mkdir -p dist
          GOOS=darwin  GOARCH=amd64  go build -C tui -o ../dist/squad-darwin-amd64  .
          GOOS=darwin  GOARCH=arm64  go build -C tui -o ../dist/squad-darwin-arm64  .
          GOOS=linux   GOARCH=amd64  go build -C tui -o ../dist/squad-linux-amd64   .
          GOOS=linux   GOARCH=arm64  go build -C tui -o ../dist/squad-linux-arm64   .
          GOOS=windows GOARCH=amd64  go build -C tui -o ../dist/squad-windows-amd64.exe .
          echo "Cross-compile OK:"
          ls -lh dist/

  # ─── CLI tests ─────────────────────────────────────────────────────────────
  test-cli:
    name: CLI Tests
    runs-on: ubuntu-latest
    needs: build-tui
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version-file: tui/go.mod
          cache-dependency-path: tui/go.sum

      - name: Build squad
        run: |
          cp -r template tui/template
          go build -C tui -o ../squad .

      - name: Configure git identity
        run: |
          git config --global user.email "ai-squad@noreply"
          git config --global user.name "AI Development Squad"

      - name: squad --version
        run: ./squad --version

      - name: squad --help
        run: ./squad --help

      - name: squad init (non-interactive)
        run: |
          mkdir /tmp/test-init
          AI_SQUAD_TEMPLATE="$GITHUB_WORKSPACE/template" ./squad init /tmp/test-init 2>&1 || true
          # Verify at minimum the binary runs without crashing
          ./squad --version

      - name: Run test suite
        run: bash tests/cli/test_ai_squad.sh

  # ─── Template validation ───────────────────────────────────────────────────
  validate-template:
    name: Validate Template
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Structure
        run: bash tests/template/test_structure.sh

      - name: Agent frontmatter
        run: bash tests/template/test_agents.sh

      - name: Skills
        run: bash tests/template/test_skills.sh

      - name: Commands
        run: bash tests/template/test_commands.sh

  # ─── CI gate ───────────────────────────────────────────────────────────────
  ci-gate:
    name: CI Gate
    runs-on: ubuntu-latest
    needs: [lint, build-tui, test-cli, validate-template]
    if: always()
    steps:
      - name: Check all jobs passed
        run: |
          if [[ "${{ needs.lint.result }}"              != "success" ||
                "${{ needs.build-tui.result }}"         != "success" ||
                "${{ needs.test-cli.result }}"          != "success" ||
                "${{ needs.validate-template.result }}" != "success" ]]; then
            echo "One or more required CI jobs failed."
            exit 1
          fi
          echo "All CI checks passed."

